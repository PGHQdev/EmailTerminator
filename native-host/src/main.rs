//! Relays CDP between the extension and the app over native messaging
//! (PLAN.md 1.3). The relay lands in M7.

fn main() {
    eprintln!("chrome-native-host: not implemented before M7");
    std::process::exit(1);
}
